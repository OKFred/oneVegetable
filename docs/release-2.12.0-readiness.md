# 2.12.0 发布准备

检查日期：2026-09-23。范围：列表、素材、库存与编辑体验优化。

## 功能与安全边界

- 统一列表搜索、筛选弹窗、三态选择、列显隐/排序与行操作菜单；列偏好保存在本机，选中记录不持久化。
- 库存成为独立工作区；产品分按可见区域加载；商品上下架回执统一进入任务中心。
- “素材”包括图片与视频，复用全屏预览和信息面板；图片仅点缩略图或预览操作才打开，服务端分页支持未知总数。
- 编辑不再自动保存或恢复商品/RFQ 本地草稿；离开有修改的表单需要确认。旧编辑数据不读取、不自动删除；批量队列、平台草稿与任务不受影响。
- 插件启动时已锁定的保险库提供可关闭解锁提示；不新增永久 Manifest 权限，不保存口令、不重放写操作。
- Node 本地与独立插件支持公网 HTTPS 地址或最大 50 MiB 本地 MP4 上传，使用已配置的私有 S3 分片暂存及平台回读。真实上传和回读已在本迭代受控验收通过；Worker 真实上传仍关闭，不提供视频删除/改名，不新增商品视频关联写入。
- 平台“已受理”不等于上传已完成；结果不明必须核对，不自动重发。已完成远端对象和平台视频不会自动删除。
- 既有 ZIP 1,500,000 字节、解包 4,100,000 字节及各分块预算保持不变。

实现与真实验收边界见 [界面迭代](list-materials-inventory-ux.md) 和 [视频上传](video-upload-backend.md)。自动化回放不代替真实平台验收。

## 升级说明

- 后端新增 `0014_video_upload_tasks.sql`，仅保存视频任务元数据和回执。Node `local-node` 在未关闭 `ONE_VEGETABLE_AUTO_MIGRATE` 时沿用启动迁移；Worker 升级须先备份 D1、执行已审核 migration 后再部署。本次发布准备不操作远端数据库。
- 已验证 2.11.0 正式 ZIP 的原始校验和，使用隔离 Chrome Profile 升级到 2.12.0；17 项检查通过，覆盖加密凭据、解锁、S3 配置、界面偏好、列偏好 V1→V2 和传输任务手工恢复。旧编辑草稿留在存储中，但不会自动恢复。
- 用户若通过 BFF 使用新版前端，应同步升级后端契约；独立插件不依赖 BFF。回退后端代码前应保留数据库备份和任务记录，不自动执行逆向迁移。

## 发布检查

- [x] 六个 workspace package、扩展 Manifest 来源和双语版本说明统一为 2.12.0。
- [x] Worker/D1：6 文件、21 项本地隔离测试通过。
- [x] 2.11.0 → 2.12.0：17 项升级检查通过。
- [x] Windows `pnpm check`：格式、Lint（0 error / 28 warning）、i18n、契约漂移、134 项离线审计、50/50 replay、类型、263 文件 / 1,638 项单测及三端构建通过。
- [x] 最终 Web/正式 MV3：91 项通过；BFF replay：2 项通过；Node 凭据双语：2 项通过。模板完整操作链另连续运行 5 次通过。
- [x] 正式 ZIP、校验和、无凭据/Mock/源码映射扫描及商店合规通过；额外扫描 `.env` / `.env.free` 的四个本机账号值，没有写入包中。
- [x] 更新八张中英文截图并逐张查看：1280×800，来自未配置账号的正式扩展，不使用业务 Mock 冒充，不包含账号或密钥。
- [x] 发布准备已分批提交。合并目标为 `staging` / `master`；远端同步及 CI 状态以本次交付的分支引用为准，不生成正式 Tag。

正式 ZIP 为 **1,127,877 字节 / 171 文件**，SHA-256：`3f6c11bee4383953da915ea06fa752a4826e64efab28c14eeca3d160182b286c`。解包 4,098,270 字节、后台 99,287 字节、入口 eager JS 190,036 字节、i18n 306,587 字节，全部符合原预算。旧 2.11.0 ZIP 的校验和仍与原发布一致。本机日志位于忽略目录 `artifacts/release-2.12.0-*`。

发布前修正了两处回归：模板弹窗关闭时过早重置标题，导致退场 DOM 被误当成新弹窗；现在下次打开才重置，测试使用精确定位及真实点击，保留卸载断言。Node 凭据 E2E 同步使用改名后的“导入图片素材”，中英文两项均已通过。能力目录更换用户文案后同步了旧断言，并补充英文检查；修正后完整 91 项重新通过。未增加超时或跳过测试。

## 商店交付

- 正式 ZIP：`artifacts/one-vegetable-v2.12.0-chrome-mv3.zip`，同目录提供 `.sha256`。
- 双语介绍及权限披露：`store-listing/zh_CN.md`、`store-listing/en.md`、`store-listing/listing.json`。
- 截图：`store-listing/assets/screenshots/zh-CN` 和 `en-US`，各四张 1280×800；图标与 Small promo tile 沿用。
- 中英文隐私政策涵盖 S3 视频暂存、任务元数据和不再恢复编辑草稿，保留非官方关联声明。
- 审核步骤见 [商店提交清单](store-submission.md)；不要提供开发者真实账号或授权包。
- 商店上传前确认后台没有同版本或更高版本，使用正式 `chrome-mv3` ZIP，不能上传 `chrome-mv3-dev` 或整个项目。

本轮仅准备发版：不自动提交商店、部署 Worker 或创建正式 GitHub Release/Tag。上传、提交审核、上架、网站部署分别确认，不能互相推断。
