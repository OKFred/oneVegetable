# 一根青菜隐私政策

生效日期：2026-08-13  
适用版本：2.0.0

## 单一用途

一根青菜是一款由用户主动操作的 Alibaba.com 国际站本地运营工作台，用于管理商品、图库（图片银行）、RFQ、交易、国际物流和相关开放平台能力。扩展不提供广告、跟踪或与该用途无关的数据服务。

## 处理的数据

- 用户主动填写的 App Key、App Secret、Access Token、签名算法和网关地址；
- 用户主动创建的商品 Schema 草稿和 RFQ 报价草稿；
- 从用户 Alibaba.com 开放平台账号按需查询的商品、图库、RFQ、交易、物流和数据洞察响应；
- 最近 100 条会话级脱敏诊断，包括操作名、耗时、错误码和可用的 traceId。

凭证与设置保存在扩展的 `chrome.storage.local`，草稿保存在扩展自身的 `localStorage`。脱敏诊断保存在 `chrome.storage.session`，浏览器会话结束后不作为长期日志保留。扩展不运行自有分析或广告服务，也不把这些数据发送到开发者自有服务器。

## 数据使用与传输

真实查询仅在用户操作后，由扩展 service worker 通过 HTTPS 向用户配置的 Alibaba.com 开放平台网关发送。外部图片转存仅在用户明确操作时下载指定公共 HTTP(S) 图片，再上传到用户自己的国际站图库。

扩展对用户数据的使用遵守 Chrome Web Store User Data Policy（包括 Limited Use 要求）：数据只用于提供或改进公开说明的单一用途，不用于个性化广告、信用评估或数据转售。除非用户明确授权技术支持、安全调查或法律要求，开发者不会允许人工读取用户数据。

## 权限说明

- `storage`：保存本地凭证、设置、首次使用状态和会话诊断；
- `https://eco.taobao.com/*`：调用 Alibaba.com 国际站正式 HTTPS 开放平台网关；
- 可选 `http://*/*`、`https://*/*`：只在用户配置自定义网关或主动转存外部图片时，按具体主机请求 Chrome 授权。用户可以在设置页逐项撤销。

扩展不申请 cookies、浏览历史、标签页或必选 `<all_urls>` 权限。所有真实写能力在取得账号并逐方法完成 smoke test 前保持关闭。

## 数据控制与保留

设置页可以查看不包含具体值的数据清单、导出该清单、清空会话诊断、撤销额外主机权限，以及彻底删除扩展的凭证、设置、草稿和诊断。卸载扩展也会删除 Chrome 管理的扩展本地存储。

## 安全与限制

扩展页面采用 Manifest V3 CSP，不执行远程代码。App Secret 仅由扩展 service worker 用于签名，但浏览器扩展无法从根本上隐藏本机中的密钥；高安全要求应迁移到用户控制的 BFF。当前版本没有真实账号验收，Mock 与自动化测试不代表 Alibaba.com 已接受真实请求。

## 联系与变更

项目主页和问题反馈：[github.com/OKFred/oneVegetable](https://github.com/OKFred/oneVegetable)。如果数据实践发生实质变化，扩展会在处理新增数据前更新显著披露并重新取得用户确认。
