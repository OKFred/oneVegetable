# 2.6.0 图库传输任务中心

状态：开发中。分支 `codex/gallery-transfer-task-recovery` 从包含 2.5.0 主线的 staging 创建。

## 固定边界

- ZIP/S3 导入导出共享应用级执行器；关闭弹窗与切页继续，刷新或关闭标签页停止调度，恢复需用户确认。
- IndexedDB 只保存版本化任务元数据；发送前落盘，未知结果不自动重发。成功回执与目标位置核对分开。
- Web Locks 同 Origin 单任务执行；不支持锁时拒绝执行，不自动接管其他窗口。
- ZIP 恢复重新选择原包；S3 保存确定目标 key，重试前核对源指纹与目标内容。密钥、图片和完整响应不存任务记录。
- 账号/配置上下文由后台核对，不替代权限、CSRF 或 mutation flag。
- 不新增永久扩展权限，不自动部署 Worker、发布商品或提交商店。

## 分步交付

1. 已完成：2.5.0 用户确认上架及原生权限补验记录收尾。
2. 待完成：任务模型、校验、IndexedDB、上下文接口及测试。
3. 待完成：应用级执行器、跨窗口互斥、持久回执及恢复。
4. 待完成：ZIP/S3 适配、完整分页核对与不确定结果保护。
5. 待完成：任务抽屉、全局进度、双语及本地数据清理。
6. 待完成：全量检查、双端真实验收及 2.6.0 候选包。

真实验收前确认独立测试存储前缀与图库目标；不修改商品，不删除远端测试素材。
